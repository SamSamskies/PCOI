<% filtered_jobs.each do |job| %>
  <tr>
    <td><%= "#{job.title}" %></td>
    <td><%= "#{job.sector.name}" %></td>
    <td><%= "#{prettify_date(job.departure_date)}" %></td>
    <td><%= "#{job.skills}" %></td>
    <td><%= "#{job.country_id}" %></td>
  </tr>
<% end %>

